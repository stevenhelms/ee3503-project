// This is an ES6 function.
const myName = name => {
    console.log(name);   
}

/*
This is an annoying array.
*/
let arr = [5,4,3,2,9];
console.log(arr);

for (let x=0; x < arr.length; x++) {
    console.log(arr[x]);
}

myName("Dilbert");

let obj = {
    "name": 'Ford',
    'age': 24
};console.log(obj);console.log(obj.name);let objarr=[];objarr.push(obj);objarr.push(obj);objarr.push(obj);

console.log(objarr);